# Next.js public API URL (FastAPI backend)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Prisma PostgreSQL (for mock/seed and server-side logic). Use a separate DB to avoid clashing with FastAPI.
# Example: same host, different database name.
PRISMA_DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nexus_prisma?schema=public"
